# Cargo configuration for local development
# Note: sccache wrapper removed - CI uses Swatinem/rust-cache@v2
# For local dev, set RUSTC_WRAPPER=sccache in your shell instead

[profile.dev]
opt-level = 0

[profile.test]
incremental = true
