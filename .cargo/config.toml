# Cargo configuration for local development
#
# Build optimizations (sccache, mold, sparse registry) are configured
# globally in ~/.cargo/config.toml — they are machine-level, not project-level.
# CI uses Swatinem/rust-cache@v2.
