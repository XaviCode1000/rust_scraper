name: 'Setup System Dependencies'
description: 'Install system dependencies (liblz4-dev for zvec-sys FFI)'

runs:
  using: 'composite'
  steps:
    - name: Install system dependencies
      shell: bash
      run: |
        sudo apt-get update
        sudo apt-get install -y liblz4-dev
